'use strict';

describe('Service: Pollinator', function () {

/*  // load the service's module
  beforeEach(module('flujogenico20App'));

  // instantiate service
  var Pollinator;
  beforeEach(inject(function (_Pollinator_) {
    Pollinator = _Pollinator_;
  }));

  it('should do something', function () {
    expect(!!Pollinator).toBe(true);
  });*/

});
